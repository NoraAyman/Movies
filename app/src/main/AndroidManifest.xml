<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nora.movies">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PopularMoviesActivity"></activity>
        <activity android:name=".TopRatedActivity"></activity>
        <activity android:name=".NowPlayingActivity"></activity>
        <activity android:name=".PopularPersonActivity"></activity>
        <activity android:name=".SearchableMovieActivity"></activity>
        <activity android:name=".SearchablePersonActivity"></activity>
        <activity android:name=".OpenMovieLinkActivity"></activity>
        <activity android:name=".OpenPersonLinkActivity"></activity>


    </application>

</manifest>